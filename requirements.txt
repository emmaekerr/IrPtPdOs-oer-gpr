# Python 3.9
# Replace the lower bounds below with the exact versions you actually ran, from:
#   pip freeze | grep -iE "numpy|pandas|matplotlib|scikit-learn|shap"
# Pinning exact versions (e.g. scikit-learn==1.3.2) makes the results reproducible.
numpy>=1.21
pandas>=1.3
matplotlib>=3.4
scikit-learn>=1.0
shap>=0.41
jupyter
